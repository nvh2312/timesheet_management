export enum Role {
    User = 'user',
    PM = "pm",
    Admin = 'admin',
}

export enum Status {
    INIT = 'init',
    PENDING = "pending",
    APPROVED = 'approved',
    REJECTED = 'rejected',
}
export const ROLES_KEY = 'roles';
export const HOURS_WORKED = 0;
